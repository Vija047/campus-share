# Frontend Environment Variables
# Copy this to .env.local for development

# API Base URL (optional - will auto-detect if not set)
VITE_API_URL=http://localhost:5000/api

# Socket.IO URL (optional - will auto-detect if not set)
VITE_SOCKET_URL=http://localhost:5000

# Production URLs (automatically used when building for production)
# VITE_API_URL=https://campus-share-apwi.vercel.app/api
# VITE_SOCKET_URL=https://campus-share-apwi.vercel.app